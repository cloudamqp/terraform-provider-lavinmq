---
page_title: "Provider: LavinMQ"
description: |-
  The LavinMQ provider is used to interact with the LavinMQ HTTP API for managing messaging infrastructure.
---

# LavinMQ Provider

The LavinMQ provider enables Terraform to manage LavinMQ resources through its HTTP API. LavinMQ is a high-performance message broker that implements the AMQP protocol.

Use the provider to manage:
- Bindings
- Exchanges
- Federation upstreams
- Permissions
- Policies
- Publish messages
- Queue actions
- Queues
- Shovels
- Users
- Virtual hosts

## Example Usage

```terraform
terraform {
  required_providers {
    lavinmq = {
      source = "cloudamqp/lavinmq"
    }
  }
}

provider "lavinmq" {
  baseurl  = "http://localhost:15672"
  username = "guest"
  password = "guest"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `baseurl` (String) BaseURL API.
- `password` (String, Sensitive) Password to access the API
- `username` (String) Username to access the API
