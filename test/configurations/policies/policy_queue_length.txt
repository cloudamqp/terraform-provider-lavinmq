resource "lavinmq_policy" "queue_length_policy" {
	name     = "{{.PolicyName}}"
	vhost    = "{{.PolicyVhost}}"
	pattern  = "{{.PolicyPattern}}"
	apply_to = "{{.PolicyApplyTo}}"
	{{if .PolicyPriority}}priority = {{.PolicyPriority}}{{end}}
	definition = {
		"max-length" = "{{.PolicyMaxLength}}"
		{{if .PolicyMaxLengthBytes}}"max-length-bytes" = "{{.PolicyMaxLengthBytes}}"{{end}}
	}
}
