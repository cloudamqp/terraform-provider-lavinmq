resource "lavinmq_queue" "vcr_test" {
	name        = "{{.QueueName}}"
	vhost       = "{{.QueueVhost}}"
	durable     = {{.QueueDurable}}
	auto_delete = {{.QueueAutoDelete}}
	{{if .QueueArguments}}arguments = {
		{{if .ArgMessageTTL}}"x-message-ttl" = {{.ArgMessageTTL}}{{end}}
		{{if .ArgMaxLength}}"x-max-length" = {{.ArgMaxLength}}{{end}}
		{{if .ArgDeadLetterExchange}}"x-dead-letter-exchange" = "{{.ArgDeadLetterExchange}}"{{end}}
		{{if .ArgDeadLetterRoutingKey}}"x-dead-letter-routing-key" = "{{.ArgDeadLetterRoutingKey}}"{{end}}
	}{{end}}
}
